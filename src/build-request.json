{
  "kind": "build_request",
  "title": "Fix infinite loading spinner on dashboard",
  "priority": "high",
  "requirements": [
    {
      "id": "REQ-1",
      "text": "Fix the infinite loading spinner that appears on the worker dashboard, preventing users from seeing any content below the header",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-recent"
        ],
        "quotes": [
          "Nao passa daí"
        ]
      },
      "acceptanceCriteria": [
        "The dashboard loads completely without getting stuck in a loading state",
        "Task list or appropriate content displays below the CanisterTask header",
        "No infinite spinner blocks the user interface"
      ]
    },
    {
      "id": "REQ-2",
      "text": "Add error boundary and loading state timeout (5 seconds) to the WorkerDashboard component to prevent indefinite loading states",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-recent"
        ]
      },
      "acceptanceCriteria": [
        "Loading spinner times out after 5 seconds if data doesn't load",
        "Appropriate error message displays if loading fails",
        "User can retry loading manually"
      ]
    },
    {
      "id": "REQ-3",
      "text": "Investigate and fix the root cause of the dashboard data fetching failure in useQueries hooks or useActor",
      "target": "both",
      "source": {
        "messageIds": [
          "msg-recent"
        ]
      },
      "acceptanceCriteria": [
        "Dashboard stats query completes successfully",
        "Actor initialization completes without errors",
        "Console shows no React Query errors or failed network requests"
      ]
    }
  ],
  "constraints": [
    "Do not modify frontend/src/hooks/useInternetIdentity.ts",
    "Do not modify frontend/src/hooks/useActor.ts",
    "Preserve existing authentication flow"
  ],
  "nonGoals": [
    "Redesigning the dashboard layout",
    "Adding new features beyond fixing the loading issue",
    "Modifying backend logic unless directly related to the loading failure"
  ],
  "imageRequirements": {
    "required": [],
    "edits": []
  }
}